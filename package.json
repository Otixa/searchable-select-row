{
  "name": "searchable-select-row",
  "version": "1.0.2",
  "private": true,
  "description": "Home Assistant entity row for input_select and select entities that filters options as you type.",
  "type": "module",
  "license": "MIT",
  "author": "Mihail Cherkasov",
  "repository": "github:Otixa/searchable-select-row",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "check": "node --check searchable-select-row.js",
    "test": "node --test"
  }
}
